const num1 = 100;
const num2 = 50;

let val;

//Math Object
val = Math.PI;
val = Math.E;

val = Math.round(2.4);

val = Math.ceil(2.1);
val = Math.floor(2.9);

val = Math.sqrt(64);
val = Math.abs(-6);

val = Math.pow(8,2);
val = Math.min(1,2,-5);

val = Math.random();

//Random number in rang
val = Math.floor(Math.random() * 20 + 1);

console.log(val);